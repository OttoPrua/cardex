interface:
  display_name: "Perlica Low-Token Manager"
  short_description: "Portable low-token playbook for manager agents"
  default_prompt: "Use $perlica-low-token-manager to coordinate this project from durable state, delegate bounded execution, and wake only on material changes."
policy:
  allow_implicit_invocation: true
